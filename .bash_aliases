# Aliases for generally nice things
alias sagi="sudo apt-get install"
alias "update-now"="sudo apt-get update && sudo apt-get upgrade -y"
alias brb="sudo reboot now"
alias viba="vim ~/.bash_aliases && . ~/.bashrc"
alias "shut-it"="sudo shutdown now -P" 
